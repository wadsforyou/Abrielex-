import React, { useEffect, useState } from "react";
import { UserPlus } from "lucide-react";
import { base44 } from "@/api/base44Client";
import { adminList } from "@/lib/adminData";
import { PageHeader, Card, Loader, EmptyState, inputClass } from "@/components/portal/ui";
import { STAFF_ROLES } from "@/lib/roles";
export default function AdminUsers() { const [users, setUsers] = useState([]); const [profiles, setProfiles] = useState([]); const [roles, setRoles] = useState([]); const [email, setEmail] = useState(""); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(true); async function load() { try { const [u, p, r] = await Promise.all([adminList("User"), adminList("StaffProfile"), adminList("RolePermission", "-created_date", 200)]); setUsers(u.filter((item) => item.role === "admin")); setProfiles(p); setRoles(r.map((item) => item.role).filter(Boolean)); } catch (error) { setMessage(error.message || "Unable to load users"); } finally { setLoading(false); } } useEffect(() => { load(); }, []); async function invite() { try { await base44.functions.invoke("adminControl", { action: "invite_user", email, role: "admin" }); setMessage("Invitation sent."); setEmail(""); } catch (error) { setMessage(error.message || "Invitation failed"); } } async function save(user, profile, staffRole, active) { try { await base44.functions.invoke("adminControl", { action: "entity_mutation", entity: "StaffProfile", mutation: profile?.id ? "update" : "create", id: profile?.id, data: { user_id: user.id, email: user.email, full_name: user.full_name || "", staff_role: staffRole, active, is_owner: !!profile?.is_owner }, auditDetails: `Profile assigned to ${user.email}` }); load(); } catch (error) { setMessage(error.message || "Profile update failed"); } } if (loading) return <Loader />; const available = [...new Set([...STAFF_ROLES.filter((item) => item !== "owner"), ...roles.filter((item) => item !== "owner")])]; return <div><PageHeader title="Users" subtitle="Invite dashboard users and manage their roles" /><Card className="mb-6 p-5"><div className="flex gap-2"><input className={inputClass} type="email" placeholder="client@example.com" value={email} onChange={(e) => setEmail(e.target.value)} /><button onClick={invite} disabled={!email} className="inline-flex items-center gap-2 rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white disabled:opacity-50"><UserPlus className="h-4 w-4" /> Invite</button></div></Card>{message && <p className="mb-4 text-sm text-primary">{message}</p>}{users.length === 0 ? <EmptyState icon={UserPlus} title="No admin accounts" message="Invite the first dashboard user above." /> : <Card className="overflow-x-auto"><table className="w-full text-sm"><tbody className="divide-y divide-border">{users.map((user) => { const profile = profiles.find((item) => item.user_id === user.id); return <tr key={user.id}><td className="px-4 py-3">{user.email}</td><td className="px-4 py-3"><select className={inputClass} value={profile?.is_owner ? "owner" : profile?.staff_role || "administrator"} disabled={profile?.is_owner} onChange={(e) => save(user, profile, e.target.value, profile?.active !== false)}>{profile?.is_owner && <option value="owner">Owner</option>}{available.map((item) => <option key={item} value={item}>{item.replace("_", " ")}</option>)}</select></td><td className="px-4 py-3"><button onClick={() => save(user, profile, profile?.staff_role || "administrator", profile?.active === false)} className="text-primary hover:underline">{profile?.active === false ? "Activate" : "Deactivate"}</button></td></tr>; })}</tbody></table></Card>}</div>; }
